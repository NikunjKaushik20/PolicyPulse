# ─────────────────────────────────────────────────────────────────────────────
# AMD ROCm PyTorch Installation
# For AMD Instinct MI200 / MI300 series GPUs
#
# Install this BEFORE requirements.txt
# ─────────────────────────────────────────────────────────────────────────────
# Usage:
#   pip install -r requirements_amd.txt
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

--index-url https://download.pytorch.org/whl/rocm6.1
torch>=2.1.2
torchvision
torchaudio
